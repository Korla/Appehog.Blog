---
import { Image } from "astro:assets";
import BlogLayout from "../layouts/BlogLayout.astro";
import Card from "../components/card.astro";

const images = import.meta.glob("/src/assets/*.{jpeg,jpg,png,gif}");

const imageData = [
  {
    name: "generator",
    description:
      "This isn't actually a generator, but it's the closest that the headquarters can supply at the moment, some value was generated when the lumber was stored",
    link: "/posts/2018-12-12-aoc-generators/",
  },
  {
    name: "generator2",
    description:
      "There are no predators at the headquarters, just imagine that the neighbor's hens and rooster are lying in wait for prey, just like the generator is waiting for someone to call next",
    link: "/posts/2018-12-12-aoc-generators/",
  },
  {
    name: "about2",
    description:
      'Late summer 2018. From left to right, garage, "hönsahuset" and "fårahuset", greenhouse, "rökeriet", main building',
    link: "/about",
  },
  {
    name: "aoc",
    description:
      "So, how lazy was I? I took this photo from the warmth of the kitchen",
    link: "/posts/2018-12-13-stumbling-through-aoc-1",
  },
];
---

<BlogLayout>
  <ul class="mx-4">
    {
      imageData.map((image) => (
        <li class="mt-8">
          <Card>
            <div class="bg-orange-100">
              <a href={image.link}>
                <Image
                  class="w-full border-b-2 border-cyan-950"
                  style={"object-fit: cover;"}
                  src={images[`/src/assets/${image.name}.jpg`]()}
                  alt={image.name}
                />
                <div class="p-3">
                  <div class="min-h-12">{image.description}</div>
                </div>
              </a>
            </div>
          </Card>
        </li>
      ))
    }
  </ul>
</BlogLayout>
